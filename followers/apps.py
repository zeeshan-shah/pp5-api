from django.apps import AppConfig


class FollowersConfig(AppConfig):
    """
    AppConfig class for the
    'followers' app.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "followers"
